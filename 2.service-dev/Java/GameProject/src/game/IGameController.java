package game;

/**
 * IGameController interface defines the contract for a game controller that manages the game flow.
 */
public interface IGameController {
    /**
     * Starts the game by initializing the game state and handling user input.
     */
    void startGame();
}
