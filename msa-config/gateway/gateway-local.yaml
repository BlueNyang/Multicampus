spring:
  application:
    name: gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: greeting_route
              uri: lb://post-api
              predicates:
                - Path=/greeting/**

            - id: posts_route
              uri: lb://post-api
              predicates:
                - Path=/posts/**

            - id: comments_route
              uri: lb://comment-api
              predicates:
                - Path=/comments/**

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8080
